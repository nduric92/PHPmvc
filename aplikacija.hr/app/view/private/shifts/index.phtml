<h3 class="display-3">Shifts</h3>
<a href="new"><button type="button" class="btn btn-outline-primary btn-lg">Add new shift</button></a>
<br>
***
<div class="card">
    <table class="table table-striped">
    <thead>
        <tr class="table-warning">
            <th scope="col">Name</th>
            <th scope="col">Duration</th>
            <th scope="col">Action</th>
        </tr>
    </thead>
        <tbody>
            <?php foreach($data as $d):?>
                <tr>
                    <td><?=$d->name?></td>
                    <td><?=$d->duration?></td>
                    <td>
                        <a href="<?=App::config('url')?>shift/change/<?=$d->id?>"><i class="bi bi-pencil-square"></i></a>
                        <?php if($d->employees==0): ?>
                        <a onclick="return confirm('Delete <?=$d->name?> shift ?');"
                        href="<?=App::config('url')?>shift/delete/<?=$d->id?>"><i class="bi bi-trash3-fill"></i></a>
                        <?php else: ?>
                    <i title="Can not delete - Employees on shift" 
                    class="bi bi-trash3-fill" style="color: gray"></i>
                <?php endif; ?>
                    </td>
                </tr>
            <?php endforeach;?>
        </tbody>
    </table>
</div>